module.exports = {
  modulePathIgnorePatterns: [
    "<rootDir>/e/",
    "<rootDir>/x/",
    "<rootDir>/stfbank/"
  ],
  testPathIgnorePatterns: [
    "/e/",
    "/x/",
    "/stfbank/"
  ]
}

// Jest Config